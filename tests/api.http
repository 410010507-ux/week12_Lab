@baseUrl = http://localhost:3001
@token = {{login.response.body.token}}
@id = {{create.response.body.id}}

### 0) 未登入被拒
GET {{baseUrl}}/api/signup

### 1) 登入 admin
# @name login
POST {{baseUrl}}/auth/login
Content-Type: application/json

{
  "email": "admin@example.com",
  "password": "AdminPass123"
}

### 2) 用 studentA 註冊 + 登入
# @name signupA
POST {{baseUrl}}/auth/signup
Content-Type: application/json

{ "email": "a@example.com", "password": "Pass1234" }

# @name loginA
POST {{baseUrl}}/auth/login
Content-Type: application/json

{ "email": "a@example.com", "password": "Pass1234" }

### 3) studentA 建立一筆報名（ownerId = studentA）
# @name create
POST {{baseUrl}}/api/signup
Authorization: Bearer {{loginA.response.body.token}}
Content-Type: application/json

{ "name": "A同學", "email": "a1@example.com", "phone": "0912345678" }

### 4) studentB 企圖刪掉 A 的資料 → 權限不足
# @name signupB
POST {{baseUrl}}/auth/signup
Content-Type: application/json

{ "email": "b@example.com", "password": "Pass1234" }

# @name loginB
POST {{baseUrl}}/auth/login
Content-Type: application/json

{ "email": "b@example.com", "password": "Pass1234" }

DELETE {{baseUrl}}/api/signup/{{id}}
Authorization: Bearer {{loginB.response.body.token}}

### 5) admin 刪除成功
DELETE {{baseUrl}}/api/signup/{{id}}
Authorization: Bearer {{token}}
